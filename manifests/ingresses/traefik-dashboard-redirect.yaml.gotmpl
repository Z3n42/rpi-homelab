apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-dashboard
  namespace: traefik-system
spec:
  redirectRegex:
    regex: "^https?://traefik\\.lan/?$"
    replacement: "https://traefik.lan/dashboard/"
    permanent: true
