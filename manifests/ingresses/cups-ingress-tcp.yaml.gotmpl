apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: cups-tcp
  namespace: printing
spec:
  entryPoints:
    - cups
  routes:
    - match: HostSNI(`*`)
      services:
        - name: cups
          port: 631
  tls:
    passthrough: true
